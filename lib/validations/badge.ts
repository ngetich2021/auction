export const BADGE_PRICE_KES = 25;

// Free listings are only discoverable within this radius of the viewer's chosen location.
// Badged (paid) listings ignore this limit and are visible up to whatever search radius the
// viewer picked, or everywhere when no location is picked at all.
export const FREE_VISIBILITY_RADIUS_KM = 0.5;
